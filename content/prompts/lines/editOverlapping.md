---
variables:
  - anchor
---
- "{{anchor}}" — this edit and another one quote text that overlaps, so the two of them disagree about
  the same passage. Write a single edit quoting that whole passage and giving what it should become.
